[package]
name = "network"
version = "0.1.0"
edition = "2024"

[dependencies]
ostd = { workspace = true }
component = { workspace = true }
log = { workspace = true }
block = { version = "0.1.0", path = "../block" }
bit_field = "0.10.3"
driver = { version = "0.1.0", path = "../../libs/driver" }
pci = { version = "0.1.0", path = "../pci" }
smoltcp = { version = "0.12.0", default-features = false, features = ["alloc", "log", "proto-ipv4", "proto-dhcpv4", "medium-ethernet", "socket-icmp", "socket-udp", "socket-tcp", "socket-raw", "socket-dhcpv4"] }
bitflags = "2.9.4"
spin = "0.10.0"
