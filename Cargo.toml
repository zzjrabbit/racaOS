[workspace]
members = [ "raca_core/device","raca_core/framework"]
exclude = ["disk_image"]
resolver="2"

[profile.dev]
panic = "abort"

[profile.release]
lto = true
opt-level = "z"
panic = "abort"
strip = "symbols"
codegen-units = 1
